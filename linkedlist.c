#include <stdio.h>
#include <stdlib.h>

struct node {
	int x;
	struct node *next;
}

int main(void)
{
	//root node
	struct node *root

	//point root to a node struct
	root = malloc( sizeof(struct node) );

	
}
